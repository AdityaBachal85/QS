.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
node_modules/
dist/

# Local database — created by `make seed`, never shared.
# The .bak is the copy `make run` takes before it re-imports.
qs.db
qs.db-wal
qs.db-shm
qs.db.bak
